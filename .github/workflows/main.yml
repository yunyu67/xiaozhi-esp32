名称：构建 ESP32-S3 1.28-inch Music 固件: Build ESP32-S3 1.28-box Music Firmware

在::
  推::
    分支: [主]: [main]
  工作流程_派发::

工作::
  构建::
    运行于: ubuntu-latest: ubuntu-latest
    
    步骤：:
    - 名称: 检出仓库name: Checkout repository
      用途：actions/checkout@v4: actions/checkout@v4
      与：:
        子模块：递归: recursive
    
    - 名称: 使用ESP-IDF构建name: Build with ESP-IDF
      用途: espressif/esp-idf-ci-action@v1: espressif/esp-idf-ci-action@v1
      与：:
        esp_idf_version: v5.4: v5.5.2
        目标: esp32s3: esp32s3
        路径: '.': '.'
        命令: |: |
          cp -f sdkconfig.defaults sdkconfig
          idf.py set-target esp32s3
          idf.py 建立
    
    - 名称：上传固件文件name: Upload firmware artifacts
      用途: actions/upload-artifact@v4: actions/upload-artifact@v4
      如果：总是(): always()
      与：:
        名称：固件-1.28-盒子: firmware-1.28-box
        路径: |: |
          build/*.bin
          构建/闪存参数
        未找到文件：错误: error
